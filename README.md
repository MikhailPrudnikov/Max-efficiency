# MAX Efficiency - Приложение для управления продуктивностью

Полнофункциональное веб-приложение для управления задачами и повышения продуктивности с интеграцией Telegram бота.

## 📋 Содержание

- [Описание](#описание)
- [Архитектура](#архитектура)
- [Требования](#требования)
- [Быстрый старт](#быстрый-старт)
- [Запуск с Docker](#запуск-с-docker)
- [Локальная разработка](#локальная-разработка)
- [Конфигурация](#конфигурация)
- [Структура проекта](#структура-проекта)
- [API документация](#api-документация)

## 🎯 Описание

MAX Efficiency - это современное приложение для управления задачами, которое включает:

- **Веб-интерфейс** (React + Vite + TypeScript) - интуитивный интерфейс для управления задачами
- **Backend API** (Node.js + Express + PostgreSQL) - надежный сервер с REST API
- **Telegram бот** - управление задачами через мессенджер с поддержкой AI

### Основные возможности

- ✅ Управление задачами с приоритетами и дедлайнами
- 📊 Статистика и аналитика продуктивности
- 🎯 Система челленджей и достижений
- 💼 Бизнес-метрики и отчеты
- 🤖 Telegram бот с AI-ассистентом (GigaChat)
- 🎤 Голосовой ввод задач (Sber Speech)
- 🔐 Безопасная аутентификация через MAX

## 🏗 Архитектура

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   Frontend      │────▶│    Backend      │────▶│   PostgreSQL    │
│  (React/Vite)   │     │  (Express API)  │     │    Database     │
└─────────────────┘     └─────────────────┘     └─────────────────┘
                               ▲
                               │
                        ┌──────┴──────┐
                        │     Bot     │
                        │  (MAX API)  │
                        └─────────────┘
```

## 💻 Требования

### Для запуска с Docker (рекомендуется)

- Docker 20.10+
- Docker Compose 2.0+
- 2GB RAM минимум
- 5GB свободного места на диске

### Для локальной разработки

- Node.js 20.x
- npm 10.x
- PostgreSQL 16.x
- Git

## 🚀 Быстрый старт

### Запуск с Docker (рекомендуется)

1. **Клонируйте репозиторий**

```bash
git clone <repository-url>
cd maxflow-zen-49451
```

2. **Настройте переменные окружения**

```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Отредактируйте .env файл и укажите ваши значения
nano .env
```

Минимально необходимые переменные:
```env
POSTGRES_PASSWORD=ваш_пароль_бд
JWT_SECRET=ваш_секретный_ключ
BOT_TOKEN=токен_telegram_бота
```

3. **Соберите и запустите контейнеры**

```bash
# Сборка образов
docker-compose build

# Запуск всех сервисов
docker-compose up -d
```

4. **Выполните миграции базы данных**

```bash
# Запуск миграций
docker-compose exec backend npm run migrate
```

5. **Проверьте работу приложения**

- Фронтенд: http://localhost
- Backend API: http://localhost:3000
- База данных: localhost:5432

### Остановка приложения

```bash
# Остановка всех сервисов
docker-compose down

# Остановка с удалением данных
docker-compose down -v
```

## 🛠 Локальная разработка

### 1. Установка зависимостей

```bash
# Установка зависимостей фронтенда
npm install

# Установка зависимостей бэкенда
cd server
npm install
cd ..

# Установка зависимостей бота
cd bot
npm install
cd ..
```

### 2. Настройка базы данных

```bash
# Создайте базу данных PostgreSQL
createdb maxflow

# Настройте переменные окружения для бэкенда
cd server
cp .env.example .env
# Отредактируйте server/.env

# Запустите миграции
npm run migrate:dev
```

### 3. Запуск сервисов

Откройте 3 терминала:

**Терминал 1 - Backend:**
```bash
cd server
npm run dev
```

**Терминал 2 - Frontend:**
```bash
npm run dev
```

**Терминал 3 - Bot (опционально):**
```bash
cd bot
cp .env.example .env
# Отредактируйте bot/.env
npm run dev
```

## ⚙️ Конфигурация

### Переменные окружения

#### База данных
```env
POSTGRES_DB=maxflow              # Имя базы данных
POSTGRES_USER=maxflow            # Пользователь БД
POSTGRES_PASSWORD=secure_pass    # Пароль БД
```

#### Backend
```env
JWT_SECRET=your-secret-key       # Секретный ключ для JWT
CORS_ORIGIN=http://localhost     # Разрешенный origin для CORS
PORT=3000                        # Порт сервера
```

#### Telegram Bot
```env
BOT_TOKEN=your_bot_token                    # Токен Telegram бота
GIGACHAT_CLIENT_ID=your_client_id          # ID клиента GigaChat (опционально)
GIGACHAT_CLIENT_SECRET=your_secret         # Секрет GigaChat (опционально)
SBER_SPEECH_CLIENT_ID=your_client_id       # ID клиента Sber Speech (опционально)
SBER_SPEECH_CLIENT_SECRET=your_secret      # Секрет Sber Speech (опционально)
```

## 📁 Структура проекта

```
maxflow-zen-49451/
├── src/                          # Исходный код фронтенда
│   ├── components/              # React компоненты
│   ├── pages/                   # Страницы приложения
│   ├── contexts/                # React контексты
│   └── lib/                     # Утилиты и API клиент
├── server/                       # Backend сервер
│   ├── src/
│   │   ├── controllers/         # Контроллеры API
│   │   ├── routes/              # Маршруты API
│   │   ├── middleware/          # Middleware
│   │   ├── migrations/          # Миграции БД
│   │   └── config/              # Конфигурация
│   └── package.json
├── bot/                          # Telegram бот
│   ├── src/
│   │   ├── handlers/            # Обработчики команд
│   │   ├── services/            # Сервисы (AI, Speech)
│   │   └── config/              # Конфигурация
│   └── package.json
├── docker-compose.yml            # Docker Compose конфигурация
├── Dockerfile.frontend           # Dockerfile для фронтенда
├── Dockerfile.backend            # Dockerfile для бэкенда
├── Dockerfile.bot                # Dockerfile для бота
├── .env.example                  # Пример переменных окружения
├── requirements.txt              # Описание зависимостей
└── README.md                     # Этот файл
```

## 📚 API документация

### Аутентификация

**POST** `/api/auth/max`
- Аутентификация через MAX платформу
- Body: `{ "initData": "string" }`

### Задачи

**GET** `/api/tasks`
- Получить список задач
- Query params: `status`, `priority`, `date`

**POST** `/api/tasks`
- Создать новую задачу
- Body: `{ "title": "string", "description": "string", "priority": "high|medium|low" }`

**PUT** `/api/tasks/:id`
- Обновить задачу

**DELETE** `/api/tasks/:id`
- Удалить задачу

### Статистика

**GET** `/api/business/stats`
- Получить бизнес-статистику

**GET** `/api/challenges`
- Получить список челленджей

## 🐛 Решение проблем

### Backend не запускается

```bash
# Проверьте логи
docker-compose logs backend

# Проверьте подключение к БД
docker-compose exec postgres psql -U maxflow -d maxflow
```

### Ошибки аутентификации

- Убедитесь, что `JWT_SECRET` установлен
- Проверьте правильность `initData` от MAX платформы

### База данных не подключается

```bash
# Проверьте статус PostgreSQL
docker-compose ps postgres

# Проверьте логи
docker-compose logs postgres
```

## 🔧 Команды Docker

```bash
# Просмотр логов всех сервисов
docker-compose logs -f

# Просмотр логов конкретного сервиса
docker-compose logs -f backend

# Перезапуск сервиса
docker-compose restart backend

# Пересборка образа
docker-compose build --no-cache backend

# Выполнение команды в контейнере
docker-compose exec backend npm run migrate

# Очистка всех данных
docker-compose down -v
docker system prune -a
```

## 📝 Лицензия

MIT

## 👥 Поддержка

Для вопросов и предложений создавайте issue в репозитории проекта.
