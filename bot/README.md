# MAX Efficiency - Max Bot

Max бот для управления задачами с поддержкой AI-ассистента и голосового ввода.

## 🎯 Описание

Max бот предоставляет удобный интерфейс для управления задачами через мессенджер с дополнительными возможностями:

- 📝 Создание и управление задачами
- 🤖 AI-ассистент на базе GigaChat
- 🎤 Голосовой ввод задач через Sber Speech
- 📊 Просмотр статистики
- ⚡ Быстрые команды

## 🛠 Технологии

- **Node.js** 20.x - Runtime окружение
- **TypeScript** 5.x - Типизация
- **@maxhub/max-bot-api** - MAX Bot API
- **PostgreSQL** 16.x - База данных
- **Axios** - HTTP клиент
- **GigaChat API** - AI-ассистент (опционально)
- **Sber Speech API** - Распознавание речи (опционально)

## 📦 Установка

### Через Docker (рекомендуется)

```bash
# Из корневой директории проекта
docker-compose up bot
```

### Локальная установка

```bash
# Установка зависимостей
npm install

# Настройка переменных окружения
cp .env.example .env
# Отредактируйте .env файл

# Запуск в режиме разработки
npm run dev

# Сборка для production
npm run build

# Запуск production версии
npm start
```

## ⚙️ Конфигурация

### Переменные окружения (.env)

```env
# База данных
DATABASE_URL=postgresql://user:password@localhost:5432/maxflow

# Max Bot
BOT_TOKEN=your_Max_bot_token

# GigaChat (опционально)
GIGACHAT_CLIENT_ID=your_gigachat_client_id
GIGACHAT_CLIENT_SECRET=your_gigachat_client_secret

# Sber Speech (опционально)
SBER_SPEECH_CLIENT_ID=your_sber_speech_client_id
SBER_SPEECH_CLIENT_SECRET=your_sber_speech_client_secret
```

### Получение токена бота

1. Найдите [@BotFather](https://t.me/BotFather) в Max
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `.env`

### Настройка GigaChat (опционально)

1. Зарегистрируйтесь на [GigaChat](https://developers.sber.ru/gigachat)
2. Создайте приложение
3. Получите Client ID и Client Secret
4. Добавьте их в `.env`

### Настройка Sber Speech (опционально)

1. Зарегистрируйтесь на [Sber Speech](https://developers.sber.ru/speech)
2. Создайте приложение
3. Получите Client ID и Client Secret
4. Добавьте их в `.env`

## 🤖 Команды бота

### Основные команды

- `/start` - Начать работу с ботом
- `/help` - Показать справку
- `/tasks` - Показать список задач
- `/add` - Добавить новую задачу
- `/stats` - Показать статистику
- `/settings` - Настройки

### Работа с задачами

```
/add Купить продукты
/add Встреча с клиентом завтра в 15:00
/tasks today - задачи на сегодня
/tasks week - задачи на неделю
/complete 123 - отметить задачу как выполненную
/delete 123 - удалить задачу
```

### AI-ассистент

Просто отправьте сообщение боту, и он поможет:
- Создать задачу из текста
- Ответить на вопросы
- Дать рекомендации по продуктивности

### Голосовой ввод

1. Отправьте голосовое сообщение боту
2. Бот распознает речь через Sber Speech
3. Создаст задачу из распознанного текста

## 📁 Структура проекта

```
bot/
├── src/
│   ├── config/              # Конфигурация
│   │   ├── database.ts      # Настройки БД
│   │   └── env.ts           # Переменные окружения
│   ├── handlers/            # Обработчики команд
│   │   ├── basicHandlers.ts # Базовые команды
│   │   ├── taskHandlers.ts  # Команды для задач
│   │   ├── aiHandlers.ts    # AI-ассистент
│   │   └── voiceHandlers.ts # Голосовой ввод
│   ├── services/            # Сервисы
│   │   ├── gigachat.ts      # Интеграция GigaChat
│   │   └── sberSpeech.ts    # Интеграция Sber Speech
│   ├── types.ts             # TypeScript типы
│   ├── utils.ts             # Утилиты
│   ├── stateManager.ts      # Управление состоянием
│   ├── db.ts                # Работа с БД
│   └── bot.ts               # Точка входа
├── package.json
├── tsconfig.json
└── README.md
```

## 🔌 Интеграция с API

Бот использует прямое подключение к PostgreSQL для работы с данными. Структура БД совпадает с backend сервером.

### Основные операции

```typescript
// Получить задачи пользователя
const tasks = await db.query(
  'SELECT * FROM tasks WHERE user_id = $1',
  [userId]
);

// Создать задачу
await db.query(
  'INSERT INTO tasks (user_id, title, description) VALUES ($1, $2, $3)',
  [userId, title, description]
);

// Обновить статус задачи
await db.query(
  'UPDATE tasks SET status = $1 WHERE id = $2',
  ['completed', taskId]
);
```

## 🤖 AI-функции

### GigaChat интеграция

```typescript
import { GigaChatService } from './services/gigachat';

const gigachat = new GigaChatService();

// Отправить запрос
const response = await gigachat.sendMessage(userMessage);

// Извлечь задачу из текста
const task = await gigachat.extractTask(userMessage);
```

### Sber Speech интеграция

```typescript
import { SberSpeechService } from './services/sberSpeech';

const speech = new SberSpeechService();

// Распознать голосовое сообщение
const text = await speech.recognizeVoice(audioBuffer);
```

## 🔒 Безопасность

- Токен бота хранится в переменных окружения
- Валидация всех входящих данных
- Ограничение доступа к командам
- Логирование действий пользователей

## 🧪 Разработка

### Команды разработки

```bash
# Разработка с hot-reload
npm run dev

# Сборка
npm run build

# Запуск production
npm start
```

### Тестирование

```bash
# Отправить тестовое сообщение боту
curl -X POST https://api.Max.org/bot<TOKEN>/sendMessage \
  -H "Content-Type: application/json" \
  -d '{"chat_id": "<CHAT_ID>", "text": "Test message"}'
```

## 🐛 Отладка

### Проверка подключения к Max

```bash
# Проверить информацию о боте
curl https://api.Max.org/bot<TOKEN>/getMe
```

### Логирование

Бот логирует все действия в консоль:
- Входящие сообщения
- Выполненные команды
- Ошибки API

### Частые проблемы

**Бот не отвечает:**
- Проверьте правильность `BOT_TOKEN`
- Убедитесь, что бот запущен
- Проверьте логи: `docker-compose logs bot`

**Ошибки БД:**
- Проверьте `DATABASE_URL`
- Убедитесь, что PostgreSQL запущен
- Проверьте миграции

**AI не работает:**
- Проверьте настройки GigaChat/Sber Speech
- Убедитесь в наличии токенов
- Проверьте квоты API

## 📊 Мониторинг

### Метрики

- Количество активных пользователей
- Количество обработанных сообщений
- Время отклика бота
- Ошибки API

### Логи

```bash
# Просмотр логов
docker-compose logs -f bot

# Последние 100 строк
docker-compose logs --tail=100 bot
```

## 🚀 Деплой

### Production настройки

1. Используйте надежный `BOT_TOKEN`
2. Настройте мониторинг
3. Включите логирование в файл
4. Настройте автоперезапуск
5. Используйте webhook вместо polling (опционально)

### Webhook (опционально)

```typescript
// Настройка webhook
bot.setWebhook('https://your-domain.com/webhook');

// Обработка webhook
app.post('/webhook', (req, res) => {
  bot.processUpdate(req.body);
  res.sendStatus(200);
});
```

## 🔗 Полезные ссылки

- [Max Bot API](https://core.Max.org/bots/api)
- [MAX Bot API](https://developers.sber.ru/max)
- [GigaChat API](https://developers.sber.ru/gigachat)
- [Sber Speech API](https://developers.sber.ru/speech)
